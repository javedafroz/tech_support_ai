[project]
name = "tech-support-shared"
version = "0.1.0"
description = "Shared schemas and reason codes for Tech Support AI"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tech_support_shared"]
